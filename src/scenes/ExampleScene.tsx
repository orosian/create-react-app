export function ExampleScene() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-muted-foreground">
      Current scene (table / vote / night). HUD stays in StageShell.
    </div>
  )
}
